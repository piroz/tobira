apiVersion: v1
kind: Service
metadata:
  name: {{ include "tobira.fullname" . }}
  labels:
    {{- include "tobira.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tobira.selectorLabels" . | nindent 4 }}
