[main]
; tobira API server URL (the plugin appends /v1/predict automatically)
url=http://127.0.0.1:8000

; Request timeout in milliseconds
timeout=5000

; Score threshold for spam classification (0.0 - 1.0)
threshold=0.5

; Whether to reject messages classified as spam
reject_spam=true
